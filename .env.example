# Optional knobs for the council CLI.
#
# The MVP runs primarily via local CLIs (codex/claude/gemini), so no API keys are required.
# Oracle is optional and may require additional auth depending on engine/model.

# COUNCIL_MAX_ROUNDS=5
# COUNCIL_DEFAULT_CHAIR_MODE=auto

